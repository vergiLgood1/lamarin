"use server";

// Reserved for future dashboard mutations
// Examples: save dashboard preferences, pin widgets, etc.

export async function placeholder() {
  // This file is reserved for future dashboard mutation operations
  return { success: true };
}
